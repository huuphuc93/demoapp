<tr>
  <td><%= post.id %></td>
  <td><%= link_to post.user.name, user_path(post.user) %></td>
  <td><%= link_to post.title, post_path(post) %></td>
  <td><%= post.created_at %></td>
  <!-- <td>
    <%#= post.admin == true ? "Admin" : "Member" %>
  </td> -->
  <td>
    <%= link_to "Delete", admin_post_path(post), method: :delete %>
  </td>
</tr>
